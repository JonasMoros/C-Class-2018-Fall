/*
Vectors: sequential containers || Templated - data type of object stored is a parameter to the object.
Vector header file #include <vector>
Declare Vector: vector<int> temps; 
Declare Vector: vector<string> names(50);
Vectors are objects
*/ 

for(int i=0; i < name.size() i++){
    cout << names[i] << endl; 
}

string temp;
cin >> temp;
names.push_back(temp); 


int main(){
    string temp; 
    cin >> temp;
    vector<int> 
}


//finds the average temp of weeks temperature and puts it into a vector

#include <iostream>
#include <string>
#include <vector> 
using namespace std; 

int main(){
    vector<int> temps;
    for(int i = 0; i < 7; i++){
        int temp; 
        cout << "Enter Temperature: " << endl;
        cin >> temp;
        temps.push_back(temp);
    }
    int sum;
for(int i =0; i < temps.size(); i++){
sum += temps[i];
}
int avg = sum/temps.size();
cout << "The Average Temp is: " << avg <<  endl;
return 0;

}




